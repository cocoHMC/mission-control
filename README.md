# Mission Control (OpenClaw)

Self-hosted LAN/Tailnet-only task/agent/node orchestration.

## Repo
- `apps/web` — Next.js UI
- `apps/worker` — dispatcher/lease enforcement (no LLM)
- `pb` — PocketBase (binary + scripts/config)
- `docs` — runbooks

## Status
Bootstrapped repo + Next.js app + worker skeleton.
